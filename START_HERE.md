# Kernux - START HERE 🚀

## 3 Simple Questions Answered

### Q1: Ab packages kahan se download honge?

**ABHI (Now - Stubs):**
```
GitHub Release: packages tag
https://github.com/Muhammadkhan2008/kernux/releases/tag/packages

$ pkg install git → Downloads stub (240 bytes, fake for now)
```

**BAAD MEIN (48 hours later - Real packages):**
```
GitHub Release: kernux-packages repo (when you create it)
https://github.com/YOUR_USERNAME/kernux-packages/releases

$ pkg install git → Downloads REAL git (15 MB, works!)
```

---

### Q2: Commands kaise use karun?

**Download APK → Install → Open Kernux → Type commands:**

```bash
$ ls              # List files
$ pwd             # Show directory  
$ date            # Show time
$ whoami          # Show user
$ ping 8.8.8.8    # Test internet
$ curl google.com # Download webpage
$ wget file.zip   # Download file
```

**Bilkul Termux ki tarah!** 🎉

---

### Q3: Network permission - ab chalega?

**HAN!** Network permission already added! 

```bash
$ ping google.com      ✓ Works!
$ curl https://...     ✓ Works!
$ wget https://...     ✓ Works!
```

---

## READY TO USE NOW!

### Step 1: Download APK
```
GitHub → Actions → latest run → artifacts
Download: kernux-debug-apk.zip
Extract: app-debug.apk
```

### Step 2: Install
```bash
adb install app-debug.apk
```

### Step 3: Open & Play!
```bash
Tap Kernux icon
Terminal opens
Type: $ ls
✓ Works like Termux!
```

---

## What Works NOW:
- ✅ Shell commands (ls, cd, pwd, etc.)
- ✅ Network (ping, curl, wget)
- ✅ File operations (create, edit, delete)
- ✅ Package manager (pkg install, pkg list)
- ✅ Colors (16 ANSI colors)

## What's Coming (48h):
- ⏳ Real packages (git, python, gcc, vim, etc.)
- ⏳ Full development environment

---

**Download APK now and start using like Termux!** 🚀
